<header class="container">
	<nav class="navbar navbar-expand-lg">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{path('app_home')}}"><img src="{{ asset('build/images/logo.png')}}" alt="logo coupart" class="img-fluid" style="width: 12rem;"></a>
			<button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-list navbar-toggler-icon " viewbox="0 0 16 16" stroke='rgb(0, 50, 32)'>
					<path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5m0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5m0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5"/>
				</svg>
			</button>
			<div class="collapse navbar-collapse justify-content-center" id="navbarNav">
				<ul class="navbar-nav">
					<li class="nav-item">
						<a class="nav-link text-primary" aria-current="page" href="{{path('app_home')}}">Accueil</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-primary" href="{{path('app_contact')}}">Contact</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-primary" href="{{path('app_recipe')}}">Recettes</a>
					</li>
				</ul>
			</div>
			{% if user %}
				<a href="{{path('app_logout')}}" type="button" class="btn btn-primary text-success m-lg-3">Déconnexion</a>

			{% else %}
				<a href="{{path('app_login')}}" type="button" class="btn btn-primary text-success m-lg-3">Connexion</a>
			{% endif %}
		</div>
	</nav>
	<div class="row">
		<p class="fs-5 text-primary">Bienvenue
			{{user}}
			!</p>
	</div>
</header>
